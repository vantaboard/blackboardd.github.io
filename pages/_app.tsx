import type { AppProps } from 'next/app';
import './index.css';

function blackboarddGitHubIO({ Component, pageProps }: AppProps): JSX.Element {
  return <Component {...pageProps} />;
}

export default blackboarddGitHubIO;
